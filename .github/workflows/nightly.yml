name: Nightly

on:
  schedule:
    # Run at 00:00 UTC every day
    - cron: "0 0 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force build even if no changes"
        required: false
        type: boolean
        default: false

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: antlers
  RESOLVER_BINARY_NAME: antlers-resolve

jobs:
  # Check if there are new commits since last nightly
  check-changes:
    runs-on: ubuntu-24.04
    name: Check for Changes
    timeout-minutes: 5
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      short_sha: ${{ steps.check.outputs.short_sha }}
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check for changes
        id: check
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "short_sha=$SHORT_SHA" >> "$GITHUB_OUTPUT"

          if [ "${{ inputs.force }}" == "true" ]; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            echo "Force build requested"
            exit 0
          fi

          # Check if there's a nightly tag
          if git rev-parse nightly >/dev/null 2>&1; then
            NIGHTLY_SHA=$(git rev-parse nightly)
            HEAD_SHA=$(git rev-parse HEAD)

            if [ "$NIGHTLY_SHA" == "$HEAD_SHA" ]; then
              echo "should_build=false" >> "$GITHUB_OUTPUT"
              echo "No changes since last nightly"
            else
              echo "should_build=true" >> "$GITHUB_OUTPUT"
              echo "Changes detected since last nightly"
            fi
          else
            echo "should_build=true" >> "$GITHUB_OUTPUT"
            echo "No previous nightly tag found"
          fi

  # Build binaries for all platforms
  build:
    needs: check-changes
    if: needs.check-changes.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux GNU (for packages)
          - os: ubuntu-24.04
            name: linux-amd64
            target: x86_64-unknown-linux-gnu
            cross: false
          - os: ubuntu-24.04
            name: linux-aarch64
            target: aarch64-unknown-linux-gnu
            cross: true

          # Linux MUSL (static, portable)
          - os: ubuntu-24.04
            name: linux-amd64-musl
            target: x86_64-unknown-linux-musl
            cross: false
            musl: true

          # macOS
          - os: macos-15
            name: darwin-arm64
            target: aarch64-apple-darwin
            cross: false
          # TODO: Remove darwin-amd64 - Intel Macs are EOL, no support guaranteed
          - os: macos-15
            name: darwin-amd64
            target: x86_64-apple-darwin
            cross: true

          # Windows
          - os: windows-2022
            name: windows-amd64
            target: x86_64-pc-windows-msvc
            cross: false
            ext: .exe

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.name }})
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Ensure target is installed
        run: rustup target add ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.cross && runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install musl tools
        if: matrix.musl
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Cache Cargo
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-nightly-${{ matrix.target }}-${{
            hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-nightly-${{ matrix.target }}-
            ${{ runner.os }}-cargo-release-${{ matrix.target }}-
            ${{ runner.os }}-cargo-

      - name: Build release binaries
        run: cargo build --release --target ${{ matrix.target }} -p antlers-cli -p antlers-resolve
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Prepare artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          # Full CLI binary
          cp target/${{ matrix.target }}/release/${{ env.BINARY_NAME }} dist/
          cd dist
          tar -czvf ${{ env.BINARY_NAME }}-${{ matrix.name }}.tar.gz ${{ env.BINARY_NAME }}
          shasum -a 256 ${{ env.BINARY_NAME }}-${{ matrix.name }}.tar.gz > ${{ env.BINARY_NAME }}-${{ matrix.name }}.tar.gz.sha256
          rm ${{ env.BINARY_NAME }}
          cd ..
          # Minimal resolver binary
          cp target/${{ matrix.target }}/release/${{ env.RESOLVER_BINARY_NAME }} dist/
          cd dist
          tar -czvf ${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.tar.gz ${{ env.RESOLVER_BINARY_NAME }}
          shasum -a 256 ${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.tar.gz > ${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.tar.gz.sha256

      - name: Prepare artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          # Full CLI binary
          Copy-Item "target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}.exe" -Destination "dist/"
          Set-Location dist
          Compress-Archive -Path "${{ env.BINARY_NAME }}.exe" -DestinationPath "${{ env.BINARY_NAME }}-${{ matrix.name }}.zip"
          $hash = (Get-FileHash -Algorithm SHA256 "${{ env.BINARY_NAME }}-${{ matrix.name }}.zip").Hash.ToLower()
          "$hash  ${{ env.BINARY_NAME }}-${{ matrix.name }}.zip" | Out-File -Encoding ASCII "${{ env.BINARY_NAME }}-${{ matrix.name }}.zip.sha256"
          Remove-Item "${{ env.BINARY_NAME }}.exe"
          Set-Location ..
          # Minimal resolver binary
          Copy-Item "target/${{ matrix.target }}/release/${{ env.RESOLVER_BINARY_NAME }}.exe" -Destination "dist/"
          Set-Location dist
          Compress-Archive -Path "${{ env.RESOLVER_BINARY_NAME }}.exe" -DestinationPath "${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.zip"
          $hash = (Get-FileHash -Algorithm SHA256 "${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.zip").Hash.ToLower()
          "$hash  ${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.zip" | Out-File -Encoding ASCII "${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.zip.sha256"

      - name: Upload artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: binary-${{ matrix.name }}
          path: |
            dist/${{ env.BINARY_NAME }}-${{ matrix.name }}.*
            dist/${{ env.RESOLVER_BINARY_NAME }}-${{ matrix.name }}.*
          if-no-files-found: error
          retention-days: 3

  # Build DEB packages
  package-deb:
    needs: [check-changes, build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            arch: amd64
          - name: linux-aarch64
            arch: arm64

    runs-on: ubuntu-24.04
    name: Package DEB (${{ matrix.arch }})
    timeout-minutes: 15
    permissions:
      contents: read
    if: always() && needs.check-changes.outputs.should_build == 'true'

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Download binary
        id: download
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: binary-${{ matrix.name }}
          path: artifacts
        continue-on-error: true

      - name: Check if binary exists
        if: steps.download.outcome == 'success'
        id: check
        run: |
          if ls artifacts/*.tar.gz 1> /dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract binary
        if: steps.check.outputs.exists == 'true'
        run: |
          mkdir -p extracted
          for tarball in artifacts/*.tar.gz; do
            tar -xzf "$tarball" -C extracted
          done

      - name: Build DEB package
        if: steps.check.outputs.exists == 'true'
        run: |
          mkdir -p pkg/DEBIAN pkg/usr/bin
          cp extracted/${{ env.BINARY_NAME }} pkg/usr/bin/
          chmod 755 pkg/usr/bin/${{ env.BINARY_NAME }}
          # Also include minimal resolver if present
          if [ -f "extracted/${{ env.RESOLVER_BINARY_NAME }}" ]; then
            cp extracted/${{ env.RESOLVER_BINARY_NAME }} pkg/usr/bin/
            chmod 755 pkg/usr/bin/${{ env.RESOLVER_BINARY_NAME }}
          fi

          DATE=$(date +%Y%m%d)
          VERSION="0.0.0~nightly.${DATE}.${{ needs.check-changes.outputs.short_sha }}"

          # Generate control file from template
          export BINARY_NAME="${{ env.BINARY_NAME }}"
          export VERSION
          export ARCH="${{ matrix.arch }}"
          export SUMMARY_SUFFIX=" (nightly)"
          export DESCRIPTION_EXTRA=" This is an unstable nightly build."
          envsubst < packaging/deb/control.in > pkg/DEBIAN/control

          dpkg-deb --build pkg "${{ env.BINARY_NAME }}_${VERSION}_${{ matrix.arch }}.deb"
          sha256sum ./*.deb > "${{ env.BINARY_NAME }}_${VERSION}_${{ matrix.arch }}.deb.sha256"

      - name: Upload DEB package
        if: steps.check.outputs.exists == 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: deb-${{ matrix.arch }}
          path: |
            *.deb
            *.deb.sha256
          if-no-files-found: error
          retention-days: 3

  # Build RPM packages
  package-rpm:
    needs: [check-changes, build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            arch: x86_64
          - name: linux-aarch64
            arch: aarch64

    runs-on: ubuntu-24.04
    name: Package RPM (${{ matrix.arch }})
    timeout-minutes: 15
    permissions:
      contents: read
    if: always() && needs.check-changes.outputs.should_build == 'true'

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Download binary
        id: download
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: binary-${{ matrix.name }}
          path: artifacts
        continue-on-error: true

      - name: Check if binary exists
        if: steps.download.outcome == 'success'
        id: check
        run: |
          if ls artifacts/*.tar.gz 1> /dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract binary
        if: steps.check.outputs.exists == 'true'
        run: |
          mkdir -p extracted
          for tarball in artifacts/*.tar.gz; do
            tar -xzf "$tarball" -C extracted
          done

      - name: Install RPM tools
        if: steps.check.outputs.exists == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm

      - name: Build RPM package
        if: steps.check.outputs.exists == 'true'
        run: |
          mkdir -p rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          cp extracted/${{ env.BINARY_NAME }} rpmbuild/SOURCES/
          # Also include minimal resolver
          cp extracted/${{ env.RESOLVER_BINARY_NAME }} rpmbuild/SOURCES/

          DATE=$(date +%Y%m%d)
          VERSION="0.0.0"
          RELEASE="0.nightly.${DATE}.${{ needs.check-changes.outputs.short_sha }}"

          # Generate spec file from template
          export BINARY_NAME="${{ env.BINARY_NAME }}"
          export VERSION
          export RELEASE
          export SUMMARY_SUFFIX=" (nightly)"
          export DESCRIPTION_EXTRA="This is an unstable nightly build."
          envsubst < packaging/rpm/antlers.spec.in > rpmbuild/SPECS/${{ env.BINARY_NAME }}.spec

          rpmbuild --define "_topdir $(pwd)/rpmbuild" \
                   --define "_arch ${{ matrix.arch }}" \
                   --target ${{ matrix.arch }} \
                   -bb rpmbuild/SPECS/${{ env.BINARY_NAME }}.spec

          cp rpmbuild/RPMS/${{ matrix.arch }}/*.rpm ./
          sha256sum ./*.rpm > "${{ env.BINARY_NAME }}-${VERSION}-${RELEASE}.${{ matrix.arch }}.rpm.sha256"

      - name: Upload RPM package
        if: steps.check.outputs.exists == 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: rpm-${{ matrix.arch }}
          path: |
            *.rpm
            *.rpm.sha256
          if-no-files-found: error
          retention-days: 3

  # Create nightly pre-release
  release:
    needs: [check-changes, build, package-deb, package-rpm]
    runs-on: ubuntu-24.04
    name: Create Nightly Release
    timeout-minutes: 15
    permissions:
      contents: write
    if: always() && needs.check-changes.outputs.should_build == 'true' &&
      needs.build.result != 'cancelled'

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Download all artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          path: artifacts
          merge-multiple: false

      - name: Flatten artifacts
        run: |
          mkdir -p release-assets
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.sha256" -o -name "*.deb" -o -name "*.rpm" \) -exec cp {} release-assets/ \; || true
          echo "=== Release assets ==="
          ls -la release-assets/ || echo "No assets found"

      - name: Check if we have any assets
        id: assets
        run: |
          if [ -n "$(ls -A release-assets 2>/dev/null)" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "No assets to release"
          fi

      - name: Generate release notes
        if: steps.assets.outputs.exists == 'true'
        id: notes
        uses: ./.github/actions/release-notes
        with:
          version: nightly
          assets-dir: release-assets
          project-name: antlers
          project-description: A fast, reliable tool for resolving and managing JVM dependencies
          is-prerelease: "true"
          build-status: |
            {
              "build": "${{ needs.build.result }}",
              "package-deb": "${{ needs.package-deb.result }}",
              "package-rpm": "${{ needs.package-rpm.result }}"
            }

      - name: Delete existing nightly release
        if: steps.assets.outputs.exists == 'true'
        run: |
          gh release delete nightly --yes || true
          git push origin :refs/tags/nightly || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Nightly Release
        if: steps.assets.outputs.exists == 'true'
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          tag_name: nightly
          name: Nightly Build
          draft: false
          prerelease: true
          body_path: ${{ steps.notes.outputs.release-notes-file }}
          files: |
            release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Explore using MCP github tools (mcp__github__create_or_update_file) for this
      - name: Generate Eukia App Token
        if: steps.assets.outputs.exists == 'true'
        id: eukia
        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf # v2.2.1
        continue-on-error: true
        with:
          app-id: ${{ vars.EUKIA_APP_ID }}
          private-key: ${{ secrets.EUKIA_APP_PRIVATE_KEY }}
          owner: albertocavalcante
          repositories: homebrew-tap
          permission-contents: write

      - name: Update Homebrew tap
        if: steps.assets.outputs.exists == 'true' && steps.eukia.outcome == 'success'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ steps.eukia.outputs.token }}
        run: |
          # Extract SHA256 hashes from checksum files
          SHORT_SHA="${{ needs.check-changes.outputs.short_sha }}"
          SHA256_DARWIN_ARM64=$(awk '{print $1}' release-assets/antlers-darwin-arm64.tar.gz.sha256)
          SHA256_DARWIN_AMD64=$(awk '{print $1}' release-assets/antlers-darwin-amd64.tar.gz.sha256)
          SHA256_LINUX_ARM64=$(awk '{print $1}' release-assets/antlers-linux-aarch64.tar.gz.sha256)
          SHA256_LINUX_AMD64=$(awk '{print $1}' release-assets/antlers-linux-amd64.tar.gz.sha256)
          export SHORT_SHA SHA256_DARWIN_ARM64 SHA256_DARWIN_AMD64 SHA256_LINUX_ARM64 SHA256_LINUX_AMD64

          # Generate formula from template
          envsubst < packaging/homebrew/antlers-nightly.rb.in > antlers-nightly.rb

          # Get current file SHA (needed for update)
          CURRENT_SHA=$(gh api repos/albertocavalcante/homebrew-tap/contents/Formula/antlers-nightly.rb --jq '.sha' 2>/dev/null || echo "")

          # Update formula via GitHub API
          # Committer: Eukia[bot], Author: github-actions[bot]
          if [ -n "$CURRENT_SHA" ]; then
            gh api repos/albertocavalcante/homebrew-tap/contents/Formula/antlers-nightly.rb \
              --method PUT \
              -f message="Update antlers-nightly to ${SHORT_SHA}" \
              -f content="$(base64 -w0 antlers-nightly.rb)" \
              -f sha="$CURRENT_SHA" \
              -f "committer[name]=Eukia[bot]" \
              -f "committer[email]=eukia[bot]@users.noreply.github.com" \
              -f "author[name]=github-actions[bot]" \
              -f "author[email]=github-actions[bot]@users.noreply.github.com"
            echo "Updated homebrew-tap formula"
          else
            gh api repos/albertocavalcante/homebrew-tap/contents/Formula/antlers-nightly.rb \
              --method PUT \
              -f message="Add antlers-nightly formula" \
              -f content="$(base64 -w0 antlers-nightly.rb)" \
              -f "committer[name]=Eukia[bot]" \
              -f "committer[email]=eukia[bot]@users.noreply.github.com" \
              -f "author[name]=github-actions[bot]" \
              -f "author[email]=github-actions[bot]@users.noreply.github.com"
            echo "Created homebrew-tap formula"
          fi
